/**
 * Dashboard — placeholder stub.
 * Replace with real syslog analytics once ingest pipeline is wired up.
 */
export default function Dashboard() {
  return (
    <div className="space-y-4">
      <h1 className="text-xl font-bold text-white">Dashboard</h1>
      <div className="bg-gray-900 border border-gray-800 rounded-xl p-8 text-center">
        <p className="text-white text-sm">Syslog ingest dashboard — coming soon.</p>
        <p className="text-gray-600 text-xs mt-2">Configure ingest in Settings → Ingest, then check Alerts for triggered rules.</p>
      </div>
    </div>
  )
}