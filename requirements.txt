# pktLog Python dependencies

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9       # form data / file uploads

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Storage backends
clickhouse-driver>=0.2.9      # ClickHouse (default)
duckdb>=0.10.3                # DuckDB (alternate)

# App database (settings, users, alerts)
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0
alembic>=1.13.0

# Auth
python-jose[cryptography]>=3.3.0   # JWT
cryptography>=42.0.0               # Fernet encryption for stored credentials
passlib[bcrypt]>=1.7.4             # Password hashing
authlib>=1.3.0                     # Okta OIDC
itsdangerous>=2.2.0                # Session signing
httpx>=0.27.0                      # Async HTTP (Okta token exchange + notifiers)
python3-saml>=1.16.0               # Okta SAML 2.0 SP (requires xmlsec1 native lib on host)

# Background jobs
apscheduler>=3.10.4

# Config
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Notifications
aiosmtplib>=3.0.1                  # Async SMTP for email alerts
jinja2>=3.1.4                      # Webhook payload templating


# Utilities
python-dateutil>=2.9.0
ipaddress                          # stdlib, listed for clarity
